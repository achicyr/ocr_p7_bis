<%= form_with(model: contest, local: true) do |form| %>
  <% if contest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contest.errors.count, "error") %> prohibited this contest from being saved:</h2>

      <ul>
        <% contest.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    firstname: <%= form.text_field :firstname %><br/>
    lastname: <%= form.text_field :lastname %><br/>
    email: <%= form.email_field :email %><br/>
    slogan: <%= form.text_area :slogan %><br/>
    <%= form.submit %>
  </div>
<% end %>
